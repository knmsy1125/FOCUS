<% current_user.goals.each do |goal| %>
    <% goal.tasks.each do |task| %>
        <%= task.title %>
        <%= task.boby %>
        <%= link_to task.title, edit_task_path(task), class: "task_#{task.id}" %>
    <% end %>
<% end %>